#!/usr/bin/env python2.7
import os
import sys

if __name__ == "__main__":
    src_path = os.path.abspath(os.path.join(
        os.path.dirname(os.path.realpath(__file__)), '..'))
    sys.path.append(src_path)
    from src import shapeviz
    sys.exit(shapeviz.Main(sys.argv))
